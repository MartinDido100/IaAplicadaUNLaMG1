export interface AuthDataDto {
  name: string;
}

export interface AuthData {
  email: string;
  name: string;
}

export interface UserDto {
  token: string;
  email: string;
  displayName: string;
}
